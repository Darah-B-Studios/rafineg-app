import React from 'react'
import { View, Text } from 'react-native'

const NavDrawerItem: React.FunctionComponent = ({iconName: string, label: string}) => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default NavDrawerItem
